import React, { useState } from 'react';
import { createRoot } from 'react-dom/client';
import { ChatDemo } from './components/ChatDemo';

createRoot(document.getElementById('root')!).render(<ChatDemo />);
